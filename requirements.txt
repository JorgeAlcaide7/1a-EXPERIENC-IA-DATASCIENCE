# Dependencias del proyecto 1a-EXPERIENC-IA-DATASCIENCE
# Versiones fijadas con las que se generaron df_processed.csv y modelo_final.joblib.
# Instalar con:  pip install -r requirements.txt

# --- Núcleo: modelado y aplicación ---
streamlit==1.58.0          # app interactiva (app/app.py)
pandas==3.0.3              # manipulación de datos
numpy==2.4.6               # cálculo numérico
scikit-learn==1.8.0        # HistGradientBoostingRegressor (debe coincidir para cargar el .joblib)
joblib==1.5.3              # serialización del modelo
matplotlib==3.10.9         # gráficos
seaborn==0.13.2            # gráficos estadísticos
holidays==0.97             # calendario de festivos de España
python-dateutil==2.9.0.post0  # relativedelta en el feature engineering

# --- Notebooks (entrenamiento.ipynb, forecasting.ipynb) ---
jupyter==1.1.1
notebook==7.5.6
ipython==9.14.0
